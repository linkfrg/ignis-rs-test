[package]
name = "notifications_glib"
version = "0.1.0"
edition = "2024"

license.workspace = true

[dependencies]
glib.workspace = true
gio.workspace = true
tokio.workspace = true

glib_utils.workspace = true
notifications = {path = "../notifications"}

[lib]
name = "ignis_notifications_glib"
crate-type = ["cdylib"]

